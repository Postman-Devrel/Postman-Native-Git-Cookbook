#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged to check staged files
npx lint-staged

# Run Spectral linting on Postman collections if they changed
if git diff --cached --name-only | grep -qE "postman/collections/"; then
  echo "📋 Linting Postman collections with Spectral..."
  npm run lint:collections

  if [ $? -ne 0 ]; then
    echo "❌ Spectral linting failed for Postman collections"
    exit 1
  fi

  echo "✅ Postman collection linting passed!"
fi

# TEMPLATE: Uncomment and customize the sections below for your project

# # Run your project's tests
# echo "🧪 Running tests..."
# npm test
# if [ $? -ne 0 ]; then
#   echo "❌ Tests failed"
#   exit 1
# fi

# # Run Postman collections (requires API server running)
# if git diff --cached --name-only | grep -qE "postman/"; then
#   echo "📬 Running Postman collections..."
#
#   # Option 1: Run against local server (start it first)
#   # npm start &
#   # SERVER_PID=$!
#   # sleep 3
#   # postman collection run postman/collections/*.postman_collection.json
#   # kill $SERVER_PID
#
#   # Option 2: Run against staging/dev environment
#   # postman collection run \
#   #   postman/collections/*.postman_collection.json \
#   #   --environment postman/environments/dev.postman_environment.json
#
#   echo "⏭️  Postman collection execution is disabled (template mode)"
# fi

echo "✅ All pre-commit checks passed!"
